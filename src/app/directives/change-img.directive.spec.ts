import { ChangeImgDirective } from './change-img.directive';

describe('ChangeImgDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeImgDirective();
    expect(directive).toBeTruthy();
  });
});
